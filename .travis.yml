sudo: false
language: cpp
addons:
  apt:
    packages:
    - cmake
    - qttools5-dev-tools
compiler:
    - clang
before_script:
    - mkdir build
    - cd build
    - cmake ..

script: make

notifications:
  email: false